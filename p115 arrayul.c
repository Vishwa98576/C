#include<stdio.h>
#include<string.h>
main()
{
	char name[100];
	int i,x,I=0,u=0;
	char ch;
	printf("\nEnter the name=>");
	gets(name);
	
	x=strlen(name);
	{
		ch=name[i];
		if(isupper(ch))
		{
		}
		else 
		{
			printf("%c",ch);
     	}
    }
 
}

